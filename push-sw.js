self.addEventListener('push',event=>{let data={};try{data=event.data?.json()||{}}catch(_){}const url=typeof data.url==='string'&&data.url.startsWith('/')&&!data.url.startsWith('//')?data.url:'/meu-acesso';event.waitUntil(self.registration.showNotification(data.title||'Pagamento confirmado',{body:data.body||'Abra para continuar.',data:{url},tag:'payment-confirmed'}));});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=new URL(event.notification.data?.url||'/meu-acesso',self.location.origin);event.waitUntil(clients.openWindow(url.href));});
